import {CosmosConnector} from 'mainnet/cosmos';

describe('cosmost connector test', () => {
  // it('get block number', async () => {
  //   const conn = CosmosConnector(true);
  //   const [deloyer] = await conn.eth.getAccounts();
  //   console.log(deloyer);
  //   const blockNumber = await conn.eth.getBlockNumber();
  //   console.log(blockNumber);
  // });
});
